/**
 * Playwright Global Setup
 *
 * Ce fichier peut être utilisé pour :
 * - Créer des données de test dans Supabase
 * - Configurer des variables d'environnement de test
 * - Initialiser des mocks ou stubs
 *
 * Pour l'instant, il est vide car les tests actuels
 * sont principalement des tests de pages publiques.
 *
 * Usage futur (avec auth) :
 * - Créer un compte de test Supabase
 * - Générer des API keys de test
 * - Seed la database avec des données de test
 */

async function globalSetup() {
  // Future: Setup test data, auth, etc.
  console.log("🧪 E2E Setup: Tests publics (pas de setup auth pour l'instant)");
}

export default globalSetup;
